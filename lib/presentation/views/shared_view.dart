import 'package:flutter/material.dart';

class SharedView extends StatelessWidget {
  const SharedView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("Shared View"),
    );
  }
}
